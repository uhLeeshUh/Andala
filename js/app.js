import Grid from './grid';

document.addEventListener('DOMContentLoaded', () => {
  const test = document.createElement("h1");
  test.textContent = "hellow werld";
  const body = document.querySelector('body');
  body.appendChild(test);

});
